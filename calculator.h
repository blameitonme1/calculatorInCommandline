#include<bits/stdc++.h>
using namespace std;
class calculator {
public:
    double calculate(string s);
    double applyOperatorOnOperand(double num1, double num2, char opt);
    int cmpSuperiority(char a, char b);
    double factorial(double num);
};